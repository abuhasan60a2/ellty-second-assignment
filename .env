# ============================================
# Docker Compose Environment Variables
# This file is used ONLY for Docker deployment
# ============================================

# Backend Configuration (for Docker)
BACKEND_PORT=3001
MONGODB_URI=mongodb+srv://sixtyagent2_db_user:cesarerodrigo00@cluster0.8rv87zd.mongodb.net/
JWT_SECRET=secret123
JWT_EXPIRES_IN=7d
NODE_ENV=production

# Frontend Configuration (for Docker)
FRONTEND_PORT=3000
VITE_API_BASE_URL=http://localhost:3001/api

# CORS Configuration (for Docker - frontend runs on port 3000)
CORS_ORIGIN=http://localhost:3000
