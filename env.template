# ============================================
# Environment Variables Template
# ============================================
# 
# This project uses separate .env files for different environments:
# 
# 1. Root .env          → Docker Compose (docker-compose.yml)
# 2. backend/.env       → Local backend development (npm run dev)
# 3. frontend/.env      → Local frontend development (npm run dev)
#
# ============================================

# ============================================
# ROOT .env (for Docker Compose)
# ============================================
# Copy this section to root .env file

BACKEND_PORT=3001
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=7d
NODE_ENV=production

FRONTEND_PORT=3000
VITE_API_BASE_URL=http://localhost:3001/api

CORS_ORIGIN=http://localhost:3000

# ============================================
# backend/.env (for local development)
# ============================================
# Copy this section to backend/.env file

PORT=3001
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/
NODE_ENV=development
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=7d
CORS_ORIGIN=http://localhost:5173

# ============================================
# frontend/.env (for local development)
# ============================================
# Copy this section to frontend/.env file

VITE_API_BASE_URL=http://localhost:3001/api
